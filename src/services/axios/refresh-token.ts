export const refreshToken = async (): Promise<string | null> => {
  // const api = new ApiService('');

  // return await api.get('/refresh-token', ({data, status}) => {
  // 	if (status == 200 && data.accessToken) {
  // 		return data.accessToken
  // 	} else {
  // 		return ''
  // 	}
  // })
  return '';
};
